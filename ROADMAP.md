# 電気工事ツール - 未実装・検討事項まとめ

最終更新：このドキュメント作成時点の会話ログより

このファイルはコードではなく、今後の実装方針を残しておくための設計メモです。
実装済み機能の一覧ではなく、**「話には出たがまだ手をつけていないもの」**を対象にしています。

---

## 1. データ管理パイプライン（Excel/CSV → JS自動生成）

### 目的
`src/data/*.js` を直接編集するのではなく、非エンジニアでも触れる**CSVファイルを正**として、
ビルド時に自動でJSファイルを生成する仕組みにする。

### 構成イメージ
```
src/data-source/*.csv   ← 人が編集する場所（Excelで開いて保存するだけ）
        ↓ ビルド時に自動変換
src/data/*.js           ← 自動生成物（人は直接触らない）
```

### 決定事項
- Excelマスター（.xlsx）は使わない。**CSVを直接Excelで開いて編集・上書き保存**する運用（変換の手間・保存忘れ事故を減らすため）
- 変換スクリプトは `scripts/gen-data.js`（仮）としてNode.jsで実装し、`package.json` の `prebuild` に登録
  → `npm run build` のたびに自動でCSV→JS変換が走る。Vercel側の追加設定は不要
- 行の追加・並べ替えは変換スクリプトを直す必要なく、CSV編集だけで完結する設計とする
  - 一覧・プルダウン系データ（Lアングル一覧、耐火工法一覧等）→ 行順がそのまま表示順になる
  - キーで引くデータ（震度Kh表、電線データ等）→ 行順は結果に影響しない

### 電線データ（WIRE_DATA）のCSV設計ルール
階層構造（線種→芯数→外径）を持つため、正規化されたテーブル形式にする。

```csv
wireType,spec,w,h
VVF,1.6-2C,6.2,9.4
IV,1.6mm,,2.8
```

- **w列に値がある** → 平形（四角）。`{w, h}` として扱う
- **w列が空（,,）** → 丸形。h列の値がそのまま外径として扱われる

判定ロジックのイメージ：
```js
const isFlat = row.w !== "" && row.w !== undefined;
const value = isFlat
  ? { w: Number(row.w), h: Number(row.h) }
  : Number(row.h);
```

### 未着手
- 変換スクリプト本体（`scripts/gen-data.js`）は未実装
- 既存の `src/data/*.js` をCSVから生成する形に置き換える作業は未着手
- まずは1データ（`wireData` 想定）で試作 → 使い勝手を見てから全データに展開する方針（試作自体も未着手）

---

## 2. 電線データの拡充

### 2.1 対象線種の整理（100V系・強電）
- VVF（EM-EEF含む）、CV、CVT、IV、HIV、FPT、FP が対象
- このうち**幹線計算（電圧降下）で使うのは主にCV／CVTまたはFP／FPT**
- CVD等は一般には使わないため対象外
- WIRE_DATA（占積率計算用）には現状 **CV・CVT・FP・FPT が未登録**（VVF/EM-EEF/ICT/HP/IV/EM-IV/AE/EM-AE/同軸/UTPのみ実装済み）。実データの調査・追加が必要

### 2.2 STANDARD_WIRE_SIZES（幹線計算用）への列追加案
sqごとに以下を追加する方向で合意：

```csv
sq,mmPhi,weightKgPerM,ampacityA,resistanceOhmPerKm
0.9,,0.010,-,
1.25,,0.013,19,
2,1.6,0.020,27,
3.5,2.0,0.030,37,
...
```

- **重量・許容電流・抵抗値は導体の太さ（sq）に対して決まる値**のため、WIRE_DATAではなくSTANDARD_WIRE_SIZES側に持たせるのが適切と判断
- 主な対象は **CV／CVT／FP／FPT**（幹線の主戦力）
- **IV／HIVは制御盤内・機器内配線が主用途**で幹線としての使用頻度は低い認識 → sqの断面積規格のみで足りる想定（ユーザー確認待ち）
- **許容電流は絶縁種別（IV/HIV/CV等）で値が変わる** → IV基準に統一するか、種別ごとに列・テーブルを分けるかは未決定

### 2.3 同軸のインピーダンス
- インピーダンスは幹線計算（電圧降下）には無関係
- 同軸（S-5C-FB等）に意味がある値のため、**WIRE_DATA側（占積率用）に列を追加する**のが適切という整理
- 実装は未着手（列追加・実データ調査ともに未着手）

### 未決定・要確認事項（次回着手前に詰める）
1. IV/HIVについても重量・許容電流・抵抗値の列を持たせるか、sqのみで済ませるか
2. 許容電流の絶縁種別対応（IV基準で統一 or CV/CVT/FP/FPTごとに個別テーブル）
3. WIRE_DATAへのCV・CVT・FP・FPTの実データ（外径）調査

---

## 3. ケーブルラック重量計算（新規検討・未実装）

### 目的
ラック本体重量とケーブル重量を合算した**「1mあたりのラック総重量」**を算出するツール。
現状ラック耐震支持計算ツールで手入力している「ラック自重(kg/m)」「積載ケーブル質量(kg/m)」を、
この計算結果と**連動（自動入力）**できるようにする。

### 想定する構成要素
- **ラック本体重量**：ラック幅・形式ごとの単位重量テーブルが必要
  （実際の検討書では `SD-QR40 W=400` で `31.78kg/m` という実例あり。同種のテーブルを整備する必要）
- **ケーブル重量**：WIRE_DATAで管理している各線種×本数から積算
  → 上記「2.2」で追加予定の `weightKgPerM` を、占積率計算ツールの「収める電線」入力（線種・芯数・本数）と組み合わせて自動計算できる可能性がある
  → ただしWIRE_DATA（占積率用）とSTANDARD_WIRE_SIZES（幹線用）は現状別データ系列なので、重量をどちらに持たせるか設計の整理が必要
    （占積率ツールで選ぶ線種＝VVF/IV/HP/AE/同軸/UTP等はSTANDARD_WIRE_SIZESの対象外のため、重量はWIRE_DATA側に個別に持たせる方が実態に合う可能性が高い）

### ラック耐震支持計算ツールとの連携イメージ
- 占積率計算ツールで入力済みの「収める電線」構成を、ラック耐震支持計算ツール側でも参照できるようにする
- または、ラック重量計算を独立した小ツールとして用意し、算出結果を「ラック自重」「積載ケーブル質量」欄にコピーする導線を作る

### ステータス
検討のみ。データ構造（特にケーブル重量をWIRE_DATAとSTANDARD_WIRE_SIZESのどちらに持たせるか）が未確定のため、実装は未着手。

---

## 4. 将来実装：計算値のキャッシュ保持＋3ペインUI（新規検討・未実装）

### 目的
特定のツールに限り、計算した値をキャッシュ（保持）しておき、あとから参照できるようにする。

### UIイメージ
- 対象ツールを開いたときのみ、**現在の2ペイン（左：ツール一覧／右：入力・結果）を3ペインに拡張**
- 追加する右ペインに、**保持している計算結果を名称つきリストで表示**
  （例：「現場A・K-2」「現場B・1F廊下」のように名前をつけて複数件保持するイメージ）

### 想定用途
- ラック耐震支持計算のように、同じプロジェクト内で複数箇所（複数の耐震支持点）を計算することが多いツールで、結果を都度メモ代わりに残しておきたいというニーズに対応

### 未決定事項
- キャッシュの保存先（ブラウザのlocalStorage想定が濃厚だが未確定）
- 「限定項目」の対象範囲（ラック耐震支持計算のみか、他ツールにも広げるか）
- 名称の付け方（自由入力か、自動採番か）

### ステータス
アイデア段階。UI設計・保存方式ともに未着手。
